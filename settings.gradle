rootProject.name = "mirror-compiler-plugin"

include "sample"

includeBuild("idea-plugin")

includeBuild("gradle-plugin") {
    dependencySubstitution {
        substitute module("com.ahmedmourad.mirror:mirror-gradle-plugin") with project(":")
    }
}

includeBuild("compiler-plugin") {
    dependencySubstitution {
        substitute module("com.ahmedmourad.mirror:mirror-compiler-plugin") with project(":")
    }
}

includeBuild("core") {
    dependencySubstitution {
        substitute module("com.ahmedmourad.mirror:mirror-core") with project(":")
    }
}

includeBuild("annotations") {
    dependencySubstitution {
        substitute module("com.ahmedmourad.mirror:mirror-annotations") with project(":")
    }
}
