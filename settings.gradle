rootProject.name = "mirror-compiler-plugin"
include "compiler-plugin"
include "annotations"
include "sample"
include "idea-plugin"

includeBuild("gradle-plugin") {
    dependencySubstitution {
        substitute module("com.ahmedmourad.mirror:mirror-gradle-plugin") with project(":")
    }
}

includeBuild("core") {
    dependencySubstitution {
        substitute module("com.ahmedmourad.mirror:core") with project(":")
    }
}

