rootProject.name = "idea-plugin"

includeBuild("../core") {
    dependencySubstitution {
        substitute module("dev.ahmedmourad.nocopy:nocopy-core") with project(":")
    }
}

includeBuild("../compiler-plugin") {
    dependencySubstitution {
        substitute module("dev.ahmedmourad.nocopy:nocopy-compiler-plugin") with project(":")
    }
}
